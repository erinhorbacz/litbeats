# Used automatically by `npm run build` (overrides .env).
# After `wrangler deploy` prints your worker URL, put it here (keep the /token path).
REACT_APP_TOKEN_ENDPOINT=https://litbeats-token.erinhorbacz.workers.dev/token
